//! `CacheReader` — 参照専用ハンドル。
//!
//! 公開 API は `lookup_*` と `list_paths` のみ。
//! lookup 中に変更を検出した場合の内部 DELETE は実装詳細として許容する。
//!
//! `Clone` コストは `Arc` のカウントアップのみ。rayon の各タスクに自由に配布できる。

// ---------------------------------------------------------------------------
// CacheReader
// ---------------------------------------------------------------------------

pub mod cache_reader;

// ---------------------------------------------------------------------------
// 公開 API
// ---------------------------------------------------------------------------

pub mod api;

// ---------------------------------------------------------------------------
// 内部ユーティリティ
// ---------------------------------------------------------------------------

pub(crate) mod util;
